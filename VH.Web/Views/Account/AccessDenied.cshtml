@{
    ViewData["Title"] = "Acceso Denegado";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6 text-center">
            <div class="card border-danger">
                <div class="card-body">
                    <h1 class="text-danger display-1">
                        <i class="bi bi-shield-x"></i> 403
                    </h1>
                    <h3 class="card-title text-danger">Acceso Denegado</h3>
                    <p class="card-text">No tiene permisos para acceder a esta sección.</p>
                    <a href="@Url.Action("Index", "Home")" class="btn btn-primary">Volver al Inicio</a>
                </div>
            </div>
        </div>
    </div>
</div>